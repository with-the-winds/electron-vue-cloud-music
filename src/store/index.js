import { createStore } from 'vuex'
import playlist from './modules/playlist'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    playlist
  }
})
